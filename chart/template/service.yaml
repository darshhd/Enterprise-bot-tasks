apiVersion: v1
kind: Service
metadata:
  name: {{ include "darsh-http-service.fullname" . }}
  labels:
    {{- include "darsh-http-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "darsh-http-service.selectorLabels" . | nindent 4 }}
